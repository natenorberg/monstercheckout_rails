<%= form_for(@sub_item) do |f| %>
  <% if @sub_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sub_item.errors.count, "error") %> prohibited this sub_item from being saved:</h2>

      <ul>
      <% @sub_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :brand %><br>
    <%= f.text_field :brand %>
  </div>
  <div class="field">
    <%= f.label :kit %><br>
    <%= f.text_field :kit %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :is_optional %><br>
    <%= f.check_box :is_optional %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
